<?xml version="1.0"?>
<ruleset name="WordPress Plugin PHPMD ruleset"
         xmlns="http://pmd.sf.net/ruleset/1.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sf.net/ruleset/1.0.0 http://pmd.sf.net/ruleset_xml_schema.xsd"
         xsi:noNamespaceSchemaLocation="http://pmd.sf.net/ruleset_xml_schema.xsd">
    <description>Custom ruleset for WordPress plugins</description>

    <!-- Import cleancode ruleset -->
    <rule ref="rulesets/cleancode.xml">
        <exclude name="StaticAccess" />
        <exclude name="ElseExpression" />
    </rule>

    <!-- Import codesize ruleset -->
    <rule ref="rulesets/codesize.xml">
        <exclude name="ExcessiveMethodLength" />
        <exclude name="ExcessiveClassLength" />
        <exclude name="ExcessiveParameterList" />
        <exclude name="TooManyMethods" />
    </rule>

    <!-- Customized method length -->
    <rule ref="rulesets/codesize.xml/ExcessiveMethodLength">
        <properties>
            <property name="minimum" value="150" />
        </properties>
    </rule>

    <!-- Customized class length -->
    <rule ref="rulesets/codesize.xml/ExcessiveClassLength">
        <properties>
            <property name="minimum" value="2000" />
        </properties>
    </rule>

    <!-- Import controversial ruleset -->
    <rule ref="rulesets/controversial.xml">
        <exclude name="Superglobals" />
        <exclude name="CamelCaseClassName" />
        <exclude name="CamelCasePropertyName" />
        <exclude name="CamelCaseMethodName" />
    </rule>

    <!-- Import design ruleset -->
    <rule ref="rulesets/design.xml">
        <exclude name="ExitExpression" />
    </rule>

    <!-- Import naming ruleset -->
    <rule ref="rulesets/naming.xml">
        <exclude name="ShortVariable" />
        <exclude name="LongVariable" />
    </rule>

    <!-- Customized variable name length -->
    <rule ref="rulesets/naming.xml/ShortVariable">
        <properties>
            <property name="minimum" value="2" />
            <property name="exceptions" value="id,i,j,k,q,e" />
        </properties>
    </rule>

    <!-- Import unused code ruleset -->
    <rule ref="rulesets/unusedcode.xml" />
</ruleset> 